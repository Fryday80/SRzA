<?php
    $title = 'Dashboard';
    $this->headTitle($title);
/** @var  $dashboardData \Application\DataObjects\DashboardData */
$dashboardData = $dashboardData; //cleanfix easier to code

echo '<h1>' . $this->escapeHtml($title) . '</h1>';

echo $this->dashboardHelper()->getStyle();

$inside = " <p>All clicks:  564</p>
            <p>clicks:  564</p>
            <p>Active Users: $activeUserCount </p>";
echo $this->dashboardHelper()->wrapInBox($inside, 'User Statistics', 'left');
bdump($dashboardData);
echo $this->dashboardHelper()->render( $dashboardData );

echo $this->dashboardHelper()->getJS();