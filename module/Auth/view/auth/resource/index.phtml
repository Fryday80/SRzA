<?php
    $title = 'Resources';
    $this->headTitle($title);


    $modified[0] = array (
        'resource_name' => '',
        'href' => '<a href="' .  $this->url('resource', array('action'=>'add')) . '">Add new Resource</a>'
    );

    foreach ($resources as $resource => $information){
        $resources[$resource]['href']  = '<a href="' .  $this->url('resource', array('action'=>'edit', 'id' => $resource['id'])) .'">Edit</a>';
        $resources[$resource]['href'] .= '<a href="' . $this->url('resource', array('action'=>'delete', 'id' => $resource['id'])) . '">Delete</a>';
        array_push( $modified, $resources[$resource] );
    }


    echo $this->dataTable()->render(new \Application\Utility\DataTable ( array(
        'data' => $modified,
        'columns' =>    array(
            0 => array (
                'name' => 'resource_name',
                'label' => 'Resource'
            ),
            1 => array (
                'name' => 'href',
                'label' => 'Aktion'
            )
        )
    )));
?>