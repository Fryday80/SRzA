<box class="logging login loggedOut">
    <boxtitle>
        <span class="own_text_big js-L-view">Login</span>
        <button class="logoutbutton login-menu-toggle js-S-view" type="button">
            <div class="own_text_medium">Login</div>
        </button>
        <img src="/img/uikit/woodTargetSmall.png" alt="pin" class="log-pin inline-block js-L-view">
        <img src="/img/uikit/woodTargetSmallActive.png" alt="pin" class="log-pin log-pin-overlay inline-block js-L-view hidden">
    </boxtitle>
    <boxcontent class="login-form">
        <?php

        $form = $this->form;
        $form->unFocus();
        $form->prepare();
        echo $this->form($form);

            if ($this->messages){
                echo '<ul>';
                foreach($this->messages as $message)
                {
                    echo '<li>'.$message.'</li>';
                }
                echo '</ul>';
            }
        ?>
        <br/>
        <a href="/register ">Registrieren</a> <br/><br/>
        <a href="/password/reset">Passwort vergessen?</a>
    </boxcontent>
</box>