<own_text_big>Passwort zurücksetzten</own_text_big>
<div class="spacer_line"></div><br>
<?php if ($error){
    echo $message;
}
if ($pwForm !== null) {
    $pwForm->prepare();
    echo $this->form($pwForm);
}