<?php
$title = 'edit';
$this->headTitle($title);
if (isset($links)){
    foreach ($links as $link) {
        echo '<a href="' . $link['url'] . '"><button>' . $link['name'] . '</button></a>';
    }
    echo '<br/>';
    echo '<br/>';
}
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>

<?php

echo $this->form($form);